#include "main.h"

/**
 * _abs - computes absolute value of an interger
 * @n: int to compute absolute value
 * Return: The absolute value of the function
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	return (-n);

}
