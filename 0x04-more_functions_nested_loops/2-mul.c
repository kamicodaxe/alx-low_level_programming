#include "main.h"

/**
 * mul - returns the product of two intergers: a & b.
 * @a: 1st interger
 * @b: 2nd interger
 * Return: 1 - digit, 0 - is not a digit
 */
int mul(int a, int b)
{
	return (a * b);
}
